import { EditorMdDirective } from './editor-md.directive';

describe('EditorMdDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorMdDirective('md');
    expect(directive).toBeTruthy();
  });
});
